{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "buildall",
            "command": "cmake --build .",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "problemMatcher": {
                "base": "$gcc",
                "fileLocation": [
                    "relative",
                    "${workspaceFolder}/build"
                ]
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}